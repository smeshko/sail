Read the brief and write a spec for the change it asks for.

Write `spec.md` with two sections: Requirements, one per acceptance criterion, and Tasks, each naming the files it
touches. Keep the tasks small enough to implement and test one at a time.

Submit a one-sentence summary and the tasks.
