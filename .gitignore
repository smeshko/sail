# Design docs, ADRs, epics and plans stay local: they reference internal work.
docs/
CONTEXT.md
README.md
# Plan artifacts (/create-plan) stay local even if docs/ is ever published.
docs/artifacts/plans/

# Spikes are throwaway research, never committed.
spikes/

.sail-runs/
node_modules/
coverage/
.linear
.DS_Store
